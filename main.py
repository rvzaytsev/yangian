from clean import free_algebra_projection
from tabulate import tabulate
from collections import defaultdict
import hypothesis
import clean, double_bracket
import hypothesis
#clean.free_algebra_io()
hypothesis.is_shift_automorphism(5,5, 4)
# w, w_tilde = clean.canonical_words(2,2)
# #print(hypothesis.shift(w, -2))
# phi_cs, psi_cs = hypothesis.commute_shifted(w, w_tilde, -2)
# phi_sc, psi_sc = hypothesis.shift_commuted(w, w_tilde, -2)
# #phi_ok, psi_ok = clean.commute(w, w_tilde, 4)
# assert phi_cs == phi_sc
# assert psi_cs == psi_sc
# clean.pretty_print(phi_cs)
# clean.pretty_print(phi_sc)